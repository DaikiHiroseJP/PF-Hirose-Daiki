<div class="container-fluid px-5 px-sm-0">
  <div class="serchebar d-flex justify-content-center mb-3">
    <%= render 'public/searches/search' %>
  </div>
  
  <% @tag_list.each do |list|%>
  <%=list.name %>
  <%="(#{list.items.count})" %><% end %>
  
  投稿一覧画面で投稿に紐づくタグを表示
  <% @items.each do |item| %>
  <i class="fas fa-tag"><%= item.tags.map(&:name).join(', ') %></i><% end %>

  <div class="col-6 mx-auto">
  <div class='row row-cols-1 row-cols-md-3 g-4'>
    <%= render 'index', items: @items %>
  </div>
  </div>
    <%= paginate @items, theme: 'twitter-bootstrap-4' %>
</div>

